{
  "name": "@codesordinatestudio/social-auth",
  "version": "1.0.0",
  "description": "A configurable social authentication package for GitHub, Google, and Apple OAuth",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --target=bun --sourcemap --external=@octokit/oauth-app --external=@octokit/rest --external=googleapis",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "prepublishOnly": "bun run build"
  },
  "keywords": [
    "oauth",
    "social-auth",
    "github",
    "google",
    "apple",
    "authentication",
    "bun",
    "nodejs"
  ],
  "author": "codesordinatestudio",
  "license": "MIT",
  "dependencies": {
    "@octokit/oauth-app": "^8.0.1",
    "@octokit/rest": "^22.0.0",
    "@types/jsonwebtoken": "^9.0.9",
    "googleapis": "^149.0.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "bun": ">=1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md"
  ]
}
